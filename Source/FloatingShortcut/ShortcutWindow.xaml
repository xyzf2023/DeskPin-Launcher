<Window x:Class="FloatingShortcut.ShortcutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FloatingShortcut"
        Width="72"
        Height="72"
        ResizeMode="NoResize"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        Background="Transparent"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        Closing="Window_Closing">
    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="CircleRow" Height="72" />
            <RowDefinition x:Name="NameRow" Height="0" />
        </Grid.RowDefinitions>

        <Grid x:Name="CircleHost"
              Grid.Row="0"
              Width="72"
              Height="72"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">
            <Ellipse x:Name="OuterCircle"
                     Width="72"
                     Height="72"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Fill="#CC1E1E1E"
                     StrokeThickness="1"
                     Stroke="#00000000">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Stroke" Value="#00000000" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Stroke" Value="#66FFFFFF" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <Image x:Name="IconImage"
                   Width="48"
                   Height="48"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   SnapsToDevicePixels="True" />
        </Grid>

        <TextBlock x:Name="NameText"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   MaxWidth="120"
                   TextTrimming="CharacterEllipsis"
                   TextWrapping="NoWrap"
                   FontSize="11"
                   Foreground="#DDFFFFFF"
                   Visibility="Collapsed" />
    </Grid>
</Window>
